<?xml version="1.0" encoding="UTF-8"?>
<mapper namespace="com.mars.web.auth.mapper.AuthMapper">
	<select id="selectUserById" resultType="map">
	    SELECT
	        USER_ID,
	        USER_PW,
	        USER_TYPE_CD
	    FROM TB_USER
	    WHERE USER_ID = #{userId}
	</select>
</mapper>